如何鉴权
如何判断token过期
添加一个QueryUser的服务?
用户名不唯一怎么保障登录正确?
token怎么生成?登录/创建用户时创建?

改进:
- users表添加nickname字段
- 创建auth表
- 表中添加公共字段


配置文件存放在全局configs中的yaml中.
在每个服务的文件夹下拷贝config文件夹和global文件夹下的setting.
在main文件中, 添加setupSetting函数, 在Init函数中调用.